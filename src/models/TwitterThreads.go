// Code generated by SQLBoiler 4.14.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// TwitterThread is an object representing the database table.
type TwitterThread struct {
	EventID int64     `boil:"event_id" json:"event_id" toml:"event_id" yaml:"event_id"`
	TweetID string    `boil:"tweet_id" json:"tweet_id" toml:"tweet_id" yaml:"tweet_id"`
	Created time.Time `boil:"created" json:"created" toml:"created" yaml:"created"`

	R *twitterThreadR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L twitterThreadL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var TwitterThreadColumns = struct {
	EventID string
	TweetID string
	Created string
}{
	EventID: "event_id",
	TweetID: "tweet_id",
	Created: "created",
}

var TwitterThreadTableColumns = struct {
	EventID string
	TweetID string
	Created string
}{
	EventID: "TwitterThreads.event_id",
	TweetID: "TwitterThreads.tweet_id",
	Created: "TwitterThreads.created",
}

// Generated where

var TwitterThreadWhere = struct {
	EventID whereHelperint64
	TweetID whereHelperstring
	Created whereHelpertime_Time
}{
	EventID: whereHelperint64{field: "`TwitterThreads`.`event_id`"},
	TweetID: whereHelperstring{field: "`TwitterThreads`.`tweet_id`"},
	Created: whereHelpertime_Time{field: "`TwitterThreads`.`created`"},
}

// TwitterThreadRels is where relationship names are stored.
var TwitterThreadRels = struct {
}{}

// twitterThreadR is where relationships are stored.
type twitterThreadR struct {
}

// NewStruct creates a new relationship struct
func (*twitterThreadR) NewStruct() *twitterThreadR {
	return &twitterThreadR{}
}

// twitterThreadL is where Load methods for each relationship are stored.
type twitterThreadL struct{}

var (
	twitterThreadAllColumns            = []string{"event_id", "tweet_id", "created"}
	twitterThreadColumnsWithoutDefault = []string{"event_id", "tweet_id"}
	twitterThreadColumnsWithDefault    = []string{"created"}
	twitterThreadPrimaryKeyColumns     = []string{"event_id"}
	twitterThreadGeneratedColumns      = []string{}
)

type (
	// TwitterThreadSlice is an alias for a slice of pointers to TwitterThread.
	// This should almost always be used instead of []TwitterThread.
	TwitterThreadSlice []*TwitterThread
	// TwitterThreadHook is the signature for custom TwitterThread hook methods
	TwitterThreadHook func(context.Context, boil.ContextExecutor, *TwitterThread) error

	twitterThreadQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	twitterThreadType                 = reflect.TypeOf(&TwitterThread{})
	twitterThreadMapping              = queries.MakeStructMapping(twitterThreadType)
	twitterThreadPrimaryKeyMapping, _ = queries.BindMapping(twitterThreadType, twitterThreadMapping, twitterThreadPrimaryKeyColumns)
	twitterThreadInsertCacheMut       sync.RWMutex
	twitterThreadInsertCache          = make(map[string]insertCache)
	twitterThreadUpdateCacheMut       sync.RWMutex
	twitterThreadUpdateCache          = make(map[string]updateCache)
	twitterThreadUpsertCacheMut       sync.RWMutex
	twitterThreadUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var twitterThreadAfterSelectHooks []TwitterThreadHook

var twitterThreadBeforeInsertHooks []TwitterThreadHook
var twitterThreadAfterInsertHooks []TwitterThreadHook

var twitterThreadBeforeUpdateHooks []TwitterThreadHook
var twitterThreadAfterUpdateHooks []TwitterThreadHook

var twitterThreadBeforeDeleteHooks []TwitterThreadHook
var twitterThreadAfterDeleteHooks []TwitterThreadHook

var twitterThreadBeforeUpsertHooks []TwitterThreadHook
var twitterThreadAfterUpsertHooks []TwitterThreadHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *TwitterThread) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range twitterThreadAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *TwitterThread) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range twitterThreadBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *TwitterThread) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range twitterThreadAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *TwitterThread) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range twitterThreadBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *TwitterThread) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range twitterThreadAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *TwitterThread) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range twitterThreadBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *TwitterThread) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range twitterThreadAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *TwitterThread) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range twitterThreadBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *TwitterThread) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range twitterThreadAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddTwitterThreadHook registers your hook function for all future operations.
func AddTwitterThreadHook(hookPoint boil.HookPoint, twitterThreadHook TwitterThreadHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		twitterThreadAfterSelectHooks = append(twitterThreadAfterSelectHooks, twitterThreadHook)
	case boil.BeforeInsertHook:
		twitterThreadBeforeInsertHooks = append(twitterThreadBeforeInsertHooks, twitterThreadHook)
	case boil.AfterInsertHook:
		twitterThreadAfterInsertHooks = append(twitterThreadAfterInsertHooks, twitterThreadHook)
	case boil.BeforeUpdateHook:
		twitterThreadBeforeUpdateHooks = append(twitterThreadBeforeUpdateHooks, twitterThreadHook)
	case boil.AfterUpdateHook:
		twitterThreadAfterUpdateHooks = append(twitterThreadAfterUpdateHooks, twitterThreadHook)
	case boil.BeforeDeleteHook:
		twitterThreadBeforeDeleteHooks = append(twitterThreadBeforeDeleteHooks, twitterThreadHook)
	case boil.AfterDeleteHook:
		twitterThreadAfterDeleteHooks = append(twitterThreadAfterDeleteHooks, twitterThreadHook)
	case boil.BeforeUpsertHook:
		twitterThreadBeforeUpsertHooks = append(twitterThreadBeforeUpsertHooks, twitterThreadHook)
	case boil.AfterUpsertHook:
		twitterThreadAfterUpsertHooks = append(twitterThreadAfterUpsertHooks, twitterThreadHook)
	}
}

// One returns a single twitterThread record from the query.
func (q twitterThreadQuery) One(ctx context.Context, exec boil.ContextExecutor) (*TwitterThread, error) {
	o := &TwitterThread{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for TwitterThreads")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all TwitterThread records from the query.
func (q twitterThreadQuery) All(ctx context.Context, exec boil.ContextExecutor) (TwitterThreadSlice, error) {
	var o []*TwitterThread

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to TwitterThread slice")
	}

	if len(twitterThreadAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all TwitterThread records in the query.
func (q twitterThreadQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count TwitterThreads rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q twitterThreadQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if TwitterThreads exists")
	}

	return count > 0, nil
}

// TwitterThreads retrieves all the records using an executor.
func TwitterThreads(mods ...qm.QueryMod) twitterThreadQuery {
	mods = append(mods, qm.From("`TwitterThreads`"))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"`TwitterThreads`.*"})
	}

	return twitterThreadQuery{q}
}

// FindTwitterThread retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindTwitterThread(ctx context.Context, exec boil.ContextExecutor, eventID int64, selectCols ...string) (*TwitterThread, error) {
	twitterThreadObj := &TwitterThread{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from `TwitterThreads` where `event_id`=?", sel,
	)

	q := queries.Raw(query, eventID)

	err := q.Bind(ctx, exec, twitterThreadObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from TwitterThreads")
	}

	if err = twitterThreadObj.doAfterSelectHooks(ctx, exec); err != nil {
		return twitterThreadObj, err
	}

	return twitterThreadObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *TwitterThread) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no TwitterThreads provided for insertion")
	}

	var err error

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(twitterThreadColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	twitterThreadInsertCacheMut.RLock()
	cache, cached := twitterThreadInsertCache[key]
	twitterThreadInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			twitterThreadAllColumns,
			twitterThreadColumnsWithDefault,
			twitterThreadColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(twitterThreadType, twitterThreadMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(twitterThreadType, twitterThreadMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO `TwitterThreads` (`%s`) %%sVALUES (%s)%%s", strings.Join(wl, "`,`"), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO `TwitterThreads` () VALUES ()%s%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			cache.retQuery = fmt.Sprintf("SELECT `%s` FROM `TwitterThreads` WHERE %s", strings.Join(returnColumns, "`,`"), strmangle.WhereClause("`", "`", 0, twitterThreadPrimaryKeyColumns))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into TwitterThreads")
	}

	var identifierCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	identifierCols = []interface{}{
		o.EventID,
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, identifierCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, identifierCols...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for TwitterThreads")
	}

CacheNoHooks:
	if !cached {
		twitterThreadInsertCacheMut.Lock()
		twitterThreadInsertCache[key] = cache
		twitterThreadInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the TwitterThread.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *TwitterThread) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	twitterThreadUpdateCacheMut.RLock()
	cache, cached := twitterThreadUpdateCache[key]
	twitterThreadUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			twitterThreadAllColumns,
			twitterThreadPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update TwitterThreads, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE `TwitterThreads` SET %s WHERE %s",
			strmangle.SetParamNames("`", "`", 0, wl),
			strmangle.WhereClause("`", "`", 0, twitterThreadPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(twitterThreadType, twitterThreadMapping, append(wl, twitterThreadPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update TwitterThreads row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for TwitterThreads")
	}

	if !cached {
		twitterThreadUpdateCacheMut.Lock()
		twitterThreadUpdateCache[key] = cache
		twitterThreadUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q twitterThreadQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for TwitterThreads")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for TwitterThreads")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o TwitterThreadSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), twitterThreadPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE `TwitterThreads` SET %s WHERE %s",
		strmangle.SetParamNames("`", "`", 0, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, twitterThreadPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in twitterThread slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all twitterThread")
	}
	return rowsAff, nil
}

var mySQLTwitterThreadUniqueColumns = []string{
	"event_id",
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *TwitterThread) Upsert(ctx context.Context, exec boil.ContextExecutor, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("models: no TwitterThreads provided for upsert")
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(twitterThreadColumnsWithDefault, o)
	nzUniques := queries.NonZeroDefaultSet(mySQLTwitterThreadUniqueColumns, o)

	if len(nzUniques) == 0 {
		return errors.New("cannot upsert with a table that cannot conflict on a unique column")
	}

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzUniques {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	twitterThreadUpsertCacheMut.RLock()
	cache, cached := twitterThreadUpsertCache[key]
	twitterThreadUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, ret := insertColumns.InsertColumnSet(
			twitterThreadAllColumns,
			twitterThreadColumnsWithDefault,
			twitterThreadColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			twitterThreadAllColumns,
			twitterThreadPrimaryKeyColumns,
		)

		if !updateColumns.IsNone() && len(update) == 0 {
			return errors.New("models: unable to upsert TwitterThreads, could not build update column list")
		}

		ret = strmangle.SetComplement(ret, nzUniques)
		cache.query = buildUpsertQueryMySQL(dialect, "`TwitterThreads`", update, insert)
		cache.retQuery = fmt.Sprintf(
			"SELECT %s FROM `TwitterThreads` WHERE %s",
			strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, ret), ","),
			strmangle.WhereClause("`", "`", 0, nzUniques),
		)

		cache.valueMapping, err = queries.BindMapping(twitterThreadType, twitterThreadMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(twitterThreadType, twitterThreadMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	_, err = exec.ExecContext(ctx, cache.query, vals...)

	if err != nil {
		return errors.Wrap(err, "models: unable to upsert for TwitterThreads")
	}

	var uniqueMap []uint64
	var nzUniqueCols []interface{}

	if len(cache.retMapping) == 0 {
		goto CacheNoHooks
	}

	uniqueMap, err = queries.BindMapping(twitterThreadType, twitterThreadMapping, nzUniques)
	if err != nil {
		return errors.Wrap(err, "models: unable to retrieve unique values for TwitterThreads")
	}
	nzUniqueCols = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), uniqueMap)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.retQuery)
		fmt.Fprintln(writer, nzUniqueCols...)
	}
	err = exec.QueryRowContext(ctx, cache.retQuery, nzUniqueCols...).Scan(returns...)
	if err != nil {
		return errors.Wrap(err, "models: unable to populate default values for TwitterThreads")
	}

CacheNoHooks:
	if !cached {
		twitterThreadUpsertCacheMut.Lock()
		twitterThreadUpsertCache[key] = cache
		twitterThreadUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single TwitterThread record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *TwitterThread) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no TwitterThread provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), twitterThreadPrimaryKeyMapping)
	sql := "DELETE FROM `TwitterThreads` WHERE `event_id`=?"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from TwitterThreads")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for TwitterThreads")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q twitterThreadQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no twitterThreadQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from TwitterThreads")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for TwitterThreads")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o TwitterThreadSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(twitterThreadBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), twitterThreadPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM `TwitterThreads` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, twitterThreadPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from twitterThread slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for TwitterThreads")
	}

	if len(twitterThreadAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *TwitterThread) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindTwitterThread(ctx, exec, o.EventID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *TwitterThreadSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := TwitterThreadSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), twitterThreadPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT `TwitterThreads`.* FROM `TwitterThreads` WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 0, twitterThreadPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in TwitterThreadSlice")
	}

	*o = slice

	return nil
}

// TwitterThreadExists checks if the TwitterThread row exists.
func TwitterThreadExists(ctx context.Context, exec boil.ContextExecutor, eventID int64) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from `TwitterThreads` where `event_id`=? limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, eventID)
	}
	row := exec.QueryRowContext(ctx, sql, eventID)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if TwitterThreads exists")
	}

	return exists, nil
}

// Exists checks if the TwitterThread row exists.
func (o *TwitterThread) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return TwitterThreadExists(ctx, exec, o.EventID)
}
